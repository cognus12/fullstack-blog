import styled from 'styled-components';
import { FlexContainer } from '../../../styles/containers';

export const HashTagsWrapper = styled(FlexContainer)`
  column-gap: 5px;
  flex-wrap: wrap;
  row-gap: 5px;
`;
